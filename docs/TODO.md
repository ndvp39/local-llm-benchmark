# TODO — Task List

> **Document type:** Tasks Document (SDLC Phase 2 deliverable).
> **Project:** `on_prem_llm_lab`.
> **Companions:** `docs/PRD.md` v1.10, `docs/PLAN.md` v1.20.
> **Source authority:** `SOFTWARE_PROJECT_GUIDELINES.md` v3.00.
> **Document version:** 1.20 — 2026-06-26.
> **Status convention:** `[ ]` not started · `[~]` in progress · `[x]` done.
> **DoD:** Every task has a Definition of Done; no task is `[x]` until DoD is met.

> **Changelog (v1.10 → v1.20).** Renamed `HardwareProfiler` → `HardwareScanner` everywhere. Expanded **T-1.13 / T-1.14** to cover the three write side-effects of ADR-015 (inject `config/setup.json.hardware_constraints`, patch placeholder pair in `docs/PRD.md` + `README.md`). New tasks **T-1.16** (`init_env.py` bootstrap script — ADR-016) and **T-1.17** (placeholder markers added to PRD + README). Dedicated PRD list §7 gains **`PRD_hardware_scanner.md`** as RECOMMENDED. M1 §1.E section retitled. Existing tasks renumber-free (T-1.15 stays for AutoModel factory).
>
> **Changelog (v1.00 → v1.10).** New milestone **M2a — Plumbing test green** (was rolled into M2). New tasks for the **`AutoModel*`** initialization rule (`T-1.15`, `T-2.0`). New tasks for the **dynamic `target_models[]` JSON array** (`T-1.9`-revised, `T-3.5`-revised). API provider locked to **Anthropic** (`T-4.2`, `T-4.3` revised). §5.7 extension locked to **QLoRA training** (`T-5.3` mandatory, alternatives removed; **`T-5.0`** added to author `docs/PRD_qlora.md` BEFORE implementation). Break-even chart now **3 curves** including Cloud GPU (`T-4.4` revised). Dedicated PRD list (§7) updated: `PRD_qlora.md` is now MANDATORY (was conditional).

---

## 0. How to use this file

- Tasks are grouped by milestone (M0 → M8 — see PRD §7).
- Each task carries an **owner** placeholder (`@student` by default) and a **DoD**.
- Infra tasks (M1) are mandatory by the constitution and **MUST** be done before any business code.
- Tasks marked **(blocks code)** must complete before the next milestone may start.
- Tasks marked **🔒 constitution** are non-negotiable rules from `SOFTWARE_PROJECT_GUIDELINES.md`.
- Tasks marked **🆕 v1.10** are new in this revision.

---

## M0 — Planning approved · target 2026-06-26

- [x] **T-0.1** Write `docs/PRD.md`. — **owner:** @architect — **DoD:** present at `docs/PRD.md`, integrates assignment §§1–11 + L08 lecture; v1.10 published.
- [x] **T-0.2** Write `docs/PLAN.md`. — **owner:** @architect — **DoD:** SDK, Gatekeeper, Mixins, ADRs, C4 diagrams, folder layout; v1.10 published.
- [x] **T-0.3** Write `docs/TODO.md` (this file). — **owner:** @architect — **DoD:** all milestones decomposed; infra tasks listed; dedicated-PRD list present; v1.10 published.
- [x] **T-0.4** User reviews and approves PRD v1.10 / PLAN v1.10 / TODO v1.10. **(blocks code)** — **owner:** @user — **DoD:** explicit written approval in chat. **Approved 2026-06-26:** user said *"approved, start M1. and every task status changing, update the todo. every task finished, stop and wait for my approval to continue"* — this also became a durable process directive (per-task TODO update + stop-and-wait gate). A second approval cycle landed for PLAN v1.20 / TODO v1.20 (HardwareScanner auto-injection + `init_env.py` bootstrap): *"approved, keep guard strict and placeholder where it is"*.

---

## M1 — Infra + Setup ready · target 2026-06-27 · (blocks code)

> Mandatory infrastructure per constitution §§5, 6, 7. None of this is optional.
> **Execution order (revised 2026-06-26 by user):** T-1.1 → **T-1.10** → **T-1.2** → rest. Rationale: the hatchling wheel target in `pyproject.toml` needs `src/on_prem_llm_lab/` to exist, otherwise `uv sync` fails. IDs and DoDs are unchanged; only sequencing moves T-1.10 (skeleton) ahead of T-1.2 (lock/sync).

### 1.A — Package + environment 🔒 constitution §7.4
- [x] **T-1.1** Create `pyproject.toml` (project metadata, deps, ruff config, coverage config). — **DoD:** `uv sync` succeeds; `[project]`, `[tool.ruff]`, `[tool.coverage.run]`, `[tool.coverage.report]` sections present; `fail_under = 85`. Dependencies include `anthropic` (ADR-011), `peft`, `bitsandbytes`, `datasets` (ADR-014). **Done 2026-06-26:** file present at repo root; TOML syntax validated via `uv run` (17 runtime deps, 4 dev deps, ruff rule set & `fail_under=85` confirmed). Full `uv sync` will be exercised in T-1.2 once the `src/on_prem_llm_lab/` skeleton exists (T-1.10) — the hatch wheel target needs the package directory.
- [x] **T-1.2** Initialize `uv` and commit `uv.lock`. — **DoD:** `uv.lock` exists in the repo root; CI step `uv sync --frozen` passes. **Done 2026-06-26:** `uv lock` resolved 112 packages; `uv sync` installed 90 wheels into `.venv/` (first run ~1.5 GB download incl. torch / transformers / accelerate / bitsandbytes / scipy / pyarrow); `uv sync --frozen` re-verifies in 16 ms; `uv run python -c "import on_prem_llm_lab"` walks every one of the 27 modules cleanly. Two install issues hit and resolved — both documented in `docs/prompts_book.md` §2: (a) PEP 735 `[dependency-groups]` vs legacy `[project.optional-dependencies]` for `tool.uv.default-groups`; (b) Hatchling requires `README.md` to exist for `[project].readme` even on editable installs (placeholder README created).
- [x] **T-1.3** Ban forbidden tools. — **DoD:** repo grep finds zero occurrences of `pip install`, `python -m`, `virtualenv`, `venv` in source/scripts/docs; documented in `docs/prompts_book.md`. **Done 2026-06-26:** `tools/check_forbidden_tools.py` (96 LOC, ruff-clean) ships in two modes — default (CI gate over `src/`, `tests/`, `tools/`, `init_env.py`, `pyproject.toml`, `*.sh`, `scripts/**`, `.github/workflows/*.yml`) = **43 files scanned, 0 errors**; advisory `--include-docs` sweep surfaces 18 known constitution-discussion mentions in PRD/PLAN/TODO/prompts_book.md (not invocations). Per-line escape hatch `# ALLOW-FORBIDDEN: <reason>` supported. Full rationale in `docs/prompts_book.md` §4.

### 1.B — Linting + tests 🔒 constitution §§5, 6.1
- [x] **T-1.4** Configure `ruff` with rule set `["E","F","W","I","N","UP","B","C4","SIM"]`, `ignore = ["E501"]`, `target-version = "py310"` (or 3.11). — **DoD:** `uv run ruff check src/ tests/` returns 0 violations. **Done 2026-06-26:** Ruff config was already declared in `pyproject.toml` v1.00 (T-1.1) matching constitution §6.1 verbatim — rule set `["E","F","W","I","N","UP","B","C4","SIM"]`, `ignore = ["E501"]`, `target-version = "py311"`, `line-length = 100`. Verified with ruff 0.15.20: `uv run ruff check src/ tests/` → **All checks passed!** Also verified extended scope `uv run ruff check src/ tests/ tools/` → All checks passed (42 files); `uv run ruff format --check src/ tests/ tools/` → 42 files already formatted (one initial formatter nit on `tools/check_forbidden_tools.py` auto-fixed via `ruff format`).
- [x] **T-1.5** Configure `pytest` + `pytest-cov` with `source = ["src"]`, `omit = ["src/main.py", "*/tests/*", "src/**/gui/*"]`, `fail_under = 85`. — **DoD:** `uv run pytest` runs (initially with placeholder tests) and prints coverage line. **Done 2026-06-26:** pytest 9.1.1 + pytest-cov 7.1.0 configured via `pyproject.toml` (`[tool.pytest.ini_options]` with `--strict-markers`, `--strict-config`, `--cov`, `--cov-report=term-missing`, markers `integration` / `slow`; `[tool.coverage.run]` with `branch = true`; `[tool.coverage.report]` with `fail_under = 85`). Added `tests/unit/test_smoke.py` (30 LOC) — 2 tests: top-level import + walk-import every submodule. `uv run pytest --collect-only` → 2 collected. `uv run pytest` → **2 passed in 0.93 s**, coverage line printed for every module, `results/coverage.xml` written. Note: early-M1 coverage hit `Required test coverage of 85.0% reached. Total coverage: 100.00%` vacuously (stubs have 0 statements); becomes meaningful once real code lands (it does — at T-1.13 the gate started biting at 85.29 %).
- [x] **T-1.6** Add `tools/check_file_size.py` (fails build if any `src/**/*.py` or `tests/**/*.py` exceeds 150 LOC, ignoring blank/comment-only lines). — **DoD:** script returns non-zero on a fixture file > 150 LOC; CI step wired. **Done 2026-06-26:** `tools/check_file_size.py` (90 LOC, ruff-clean) — counts non-blank/non-pure-comment lines; default scan covers `src/**/*.py`, `tests/**/*.py`, `tools/**/*.py`, `init_env.py`. CLI flags `--root`, `--max-lines`, `--quiet`. Fixture-test DoD met via `tests/unit/test_check_file_size.py` (39 LOC) — 3 tests: counter ignores blank+comment lines, **`scan` returns rc=1 on a 200-LOC fixture file (literal DoD)**, `scan` returns rc=0 on a 50-LOC fixture. Real-tree scan: 45 files, **0 violations** (largest = `tools/check_forbidden_tools.py` at 99 LOC). "CI step wired" interpreted as: `uv run python tools/check_file_size.py` callable locally + fixture test in the pytest suite; a `.github/workflows/*.yml` job will simply call those two commands when CI lands (deferred to M7).

### 1.C — Security + config scaffolding 🔒 constitution §§4.2, 6.2, 6.3, 6.4
- [x] **T-1.7** Create `.gitignore` (incl. `.env`, `*.pem`, `*.key`, `credentials.json`, `results/logs/`, AirLLM shard dir). — **DoD:** all entries present. **Done 2026-06-26 (advanced out of order to enable first commit + push):** comprehensive `.gitignore` at repo root — Python bytecode, `.venv/`, secret patterns (`.env`, `*.pem`, `*.key`, `credentials.json`, `*_secret*`), test/lint artifacts (`coverage.xml`, `.pytest_cache/`, `.ruff_cache/`, `.mypy_cache/`), editor noise, `results/logs/*` (keeps `.gitkeep`), `airllm_shards/` + `D:/airllm_shards/` per ADR-005, `*.bak` per ADR-015 (HardwareScanner backups), HF cache spillover, Jupyter checkpoints.
- [x] **T-1.8** Create `.env-example` with placeholders: `HF_TOKEN=`, `ANTHROPIC_API_KEY=`. **No `OPENAI_API_KEY`** entry (ADR-011). — **DoD:** committed; no real secrets present. **Done 2026-06-26:** `.env-example` at repo root (819 bytes, UTF-8, no BOM). Two env vars only — `HF_TOKEN=` (with HF Hub URL + "used by `services/model_acquirer.py` via ApiGatekeeper" comment) and `ANTHROPIC_API_KEY=` (with Anthropic console URL + "used by `backends/api_backend.py` for the break-even API curve" comment). Both values empty (secret-shape sanity check passes — no real keys present). File is NOT git-ignored (`git check-ignore` returns exit 1) so it will be committed. ADR-011 honored: no `OPENAI_API_KEY` entry.
- [x] **T-1.9** 🆕 v1.10 Create `config/setup.json`, `config/rate_limits.json`, `config/api_pricing.json`, `config/logging_config.json` per PLAN §6, each with `"version": "1.00"`. **`setup.json` MUST include the `target_models` JSON array (Llama-3-8B fp16, Qwen-7B-Q4) and the `plumbing_test_model` entry (small + Q2)** (ADR-013, ADR-010). **`api_pricing.json` MUST list only Anthropic** (ADR-011). **`setup.json.economic.cloud_gpu.include_in_chart` MUST default to `true`** (ADR-012). — **DoD:** files validate against `shared/config.py` reader (added in M3). **Done 2026-06-26 (early validation; full `shared/config.py` validation deferred to T-3.8):** 4 strict-JSON files (4 387 bytes total) — `setup.json` (1 491 B; v1.00, `init` block with placeholder markers + `keep_bak`, `hardware_constraints=null`, `airllm.layer_shards_saving_path`, `plumbing_test_model = TinyLlama-Q2`, `target_models[]` = `llama3-8b-fp16` + `qwen2-7b-q4`, `economic.cloud_gpu.include_in_chart=true`); `rate_limits.json` (806 B; v1.00, 3 services: `default`, `huggingface_hub`, `anthropic_messages`); `api_pricing.json` (438 B; v1.00, **Anthropic-only** with $1/$5 per-million placeholders + `note` that T-4.3 must replace with current prices + `captured_at` + `source_url`); `logging_config.json` (1 652 B; v1.00, JSONL formatter for `lab.log` via RotatingFileHandler, human formatter for console). All four parse as strict JSON, all invariants checked, `logging.config.dictConfig` loads the logging schema cleanly.

### 1.D — Source skeleton 🔒 constitution §§1.4, 13
- [x] **T-1.10** Create folder layout from PLAN §8 with empty `__init__.py` files. **Includes** `services/plumbing_test_runner.py`, `services/qlora_trainer.py`, `shared/automodel_factory.py`. — **DoD:** `python -c "import on_prem_llm_lab"` works under `uv run`. **Executes before T-1.2 per reorder note above.** **Done 2026-06-26:** 33 src files (6 subpackages: sdk, services, backends, mixins, shared, cli + root + constants) and 8 tests files (unit/, integration/, conftest.py) created. Top-level dirs added: `data/`, `results/` (+ `logs/`), `figures/`, `notebooks/`, `tools/` (each with `.gitkeep`). Structural import verified — every one of the 27 modules under `on_prem_llm_lab` imports cleanly. `uv run`-level verification will be re-confirmed at the end of T-1.2 (uv sync).
- [x] **T-1.11** Add `src/on_prem_llm_lab/shared/version.py` with `__version__ = "1.00"`. — **DoD:** importable; unit-tested. **Done 2026-06-26:** `shared/version.py` (≤ 15 LOC) sets `__version__ = "1.00"` and exposes `__all__`. Re-exported from `on_prem_llm_lab.__init__`. 3 tests in `tests/unit/test_shared/test_version.py` (value=="1.00", shape matches `\d+\.\d{2}`, root re-export equals shared value). Coverage 100% on this module.
- [x] **T-1.12** Add stub `OnPremLlmSDK` class (constructor only, no methods yet). — **DoD:** importable; unit-tested for construction. **Done 2026-06-26:** `sdk/sdk.py` ships `OnPremLlmSDK(config_path: Path|str, env: Mapping[str,str]|None=None)` — stores `config_path` as `Path` and `env` as the passed mapping (default `{}`). Methods stubbed per PLAN §3.1 will land in T-1.14/T-2a.1/T-2.6/etc. Re-exported from `on_prem_llm_lab.sdk.__init__` and `on_prem_llm_lab.__init__`. 3 tests in `tests/unit/test_sdk/test_sdk.py` (Path construction, str+env construction, root re-export identity). All ruff + 11 pytest checks pass.

### 1.E — HardwareScanner with config + doc injection 🆕 v1.20 · ADR-015
- [x] **T-1.13** 🆕 v1.20 Implement `services/hardware_scanner.py` (TDD; renamed from `hardware_profiler.py`). — **DoD:** Test-first: returns typed `HardwareScanResult` with detected CPU/RAM/GPU/VRAM/disk/OS/Python; cross-platform Win/Linux; ≤ 150 LOC; ≥ 1 happy + 1 error test per side-effect. **Side-effects (ADR-015):** (1) injects scan payload into `config/setup.json.hardware_constraints` via atomic `write tmp → os.replace` and optional `.bak`; (2) replaces content between `<!-- HARDWARE_SPECS_PLACEHOLDER:START -->` / `<!-- HARDWARE_SPECS_PLACEHOLDER:END -->` markers in each path listed in `config.init.doc_targets` (default: `docs/PRD.md`, `README.md`) with a formatted Markdown table; (3) records per-file write status in `HardwareScanResult.write_receipts`. Idempotent (re-scan yields identical bytes modulo `captured_at`). Missing files / missing marker pair → `skipped`, never silent. **Done 2026-06-26:** Split into 4 files to break a circular import (sanctioned by constitution §2.2 — "extract model definitions"): `hardware_scanner_types.py` (68 LOC, dataclasses), `hardware_scanner_detect.py` (109 LOC, psutil + optional pynvml + disk free on AirLLM shard drive per ADR-005), `hardware_scanner_writes.py` (116 LOC, atomic writes + inject + patch + `_atomic_write` helper), `hardware_scanner.py` (113 LOC, `HardwareScanner` orchestrator + `render_markdown_table` + ISO UTC clock). 8 unit tests in `tests/unit/test_services/test_hardware_scanner.py` (147 LOC) covering detect (real + monkeypatched-missing-pynvml), inject (happy + missing-file), doc-patch (replace + no-markers + missing-file), and idempotency-modulo-clock. **19 tests passed in 2.95 s; coverage 85.29 % (first real bite of the 85 % gate)**; ruff + file-size all clean. Minor cleanup: `ruff UP017` flagged `timezone.utc` → switched to `datetime.UTC`; test file initially 155 LOC → extracted `_snap()` helper → 147 LOC.
- [x] **T-1.14** 🆕 v1.20 Wire `OnPremLlmSDK.scan_hardware()`, `OnPremLlmSDK.initialize_environment()` (composes scanner + future bootstrap steps), and CLI subcommand `initialize`. — **DoD:** `uv run on-prem-llm initialize` runs the full scan + inject + patch flow, prints a per-file summary, exits 0 on success / non-zero on any per-file failure. **Done 2026-06-26:** SDK extended (~90 LOC) — constructor now accepts `repo_root` (defaults to `Path.cwd()`); new `scan_hardware(*, detector=None, clock=None)` delegates to `HardwareScanner`; new `initialize_environment(*, detector=None, clock=None)` composes scan + an `ok` summary that flags `fail`-status receipts (skip-status receipts are NOT failures per PLAN §6.7); new `InitEnvResult` frozen dataclass re-exported from `sdk/__init__` and root package. CLI (`cli/main.py`, ~67 LOC) — typer-based with `--config` / `-c` and `--repo-root` options using the modern `Annotated` pattern (ruff-clean vs B008); no-op `@app.callback()` keeps Typer in multi-command mode so `uv run on-prem-llm initialize ...` works (single-command mode collapsed the subcommand name, caused 3 test failures during development; fixed). Added 4 SDK tests + 3 CLI tests via `typer.testing.CliRunner`. **DoD also demonstrated on the real machine** — running the CLI against the real repo (with real hardware) populated `config/setup.json.hardware_constraints` and replaced the placeholder block in both `README.md` and `docs/PRD.md` with a live Markdown table (visible in the working tree). **Gates:** ruff clean across `src/`, `tests/`, `tools/`; 53 files, 0 LOC violations; **26 tests passed in 3.28 s; coverage 91.10 %** (up from 85.29 %).

### 1.F — `AutoModel*` factory helper 🆕 v1.10 · ADR-009
- [x] **T-1.15** 🆕 v1.10 Implement `shared/automodel_factory.py` — a ≤ 50 LOC helper that resolves dtype / device_map per quantization level and returns `AutoModelForCausalLM` + `AutoTokenizer` instances. — **DoD:** ≤ 150 LOC (target ≤ 50); unit-tested with mocked `transformers`; happy + UnsupportedQuantization error paths. **Done 2026-06-27:** `src/on_prem_llm_lab/shared/automodel_factory.py` (65 LOC — over the aspirational ≤50 target, well within the hard ≤150 gate; the overshoot is the `LoadedModel` dataclass that surfaces `resolved_dtype` / `resolved_device_map` to callers + tests, plus `__all__` re-export hygiene matching the rest of `shared/*`). Surface: `load_causal_lm(model_id, quantization, *, device_map="auto", trust_remote_code=False) -> LoadedModel`, with `SUPPORTED_QUANTIZATIONS = ("fp32","fp16","q8","q4","q2","nf4")` per FR-7 and `UnsupportedQuantizationError(ValueError)` per FR-8. **Naming nit:** PRD/PLAN reference the class as `UnsupportedQuantization`; renamed to `UnsupportedQuantizationError` to satisfy ruff N818 and match the project's other exceptions (`GatekeeperError`, `PlumbingStageError`, `EnvironmentNotInitializedError`). Loader always goes through `AutoModelForCausalLM.from_pretrained(model_id, torch_dtype=dtype, device_map=device_map, low_cpu_mem_usage=True)` + `AutoTokenizer.from_pretrained(model_id, trust_remote_code=...)` — direct concrete-class imports forbidden (T-2.0 static AST guard will enforce). Tests at `tests/unit/test_shared/test_automodel_factory.py` (82 LOC): 1 unsupported-error test + 1 happy-path test (asserts both factories called once each with the right kwargs) + 6 parametrized dtype-resolution tests covering every supported label. **Gates:** ruff clean (one round of cleanup — N818 rename + 2 UP037 quote drops); 54 files, 0 LOC violations; **34 tests passed in 65 s; coverage 91.71 %** (up from 91.10 %); `automodel_factory.py` itself at 100 % statement + 100 % branch coverage. Real `transformers` is monkeypatched in tests so no model download is triggered.

### 1.G — `init_env.py` bootstrap + placeholder seeding 🆕 v1.20 · ADR-016
- [x] **T-1.16** 🆕 v1.20 Create `init_env.py` at the repo root (`uv run init_env.py`). — **DoD:** ≤ 30 LOC; delegate-only — parses args, builds `OnPremLlmSDK`, calls `sdk.initialize_environment()`, prints summary, exits non-zero on failure. **Precondition guard everywhere:** every other SDK method (`run_plumbing_test`, `run_baseline`, `run_airllm`, `run_sweep`, `run_qlora_finetune`, `economic_analysis`, `assemble_readme`) raises `EnvironmentNotInitializedError` when `config.hardware_constraints` is `null` or older than `init.max_age_hours`, with a remediation hint pointing back to `uv run init_env.py`. Integration test `tests/integration/test_env_init_required.py` covers both "missing" and "stale" cases. Integration test `tests/integration/test_hardware_scanner_writes.py` covers the three write side-effects from T-1.13. **Done 2026-06-27:** Split into a guard module + 7 SDK stubs + the bootstrap script + 3 test files. (1) `src/on_prem_llm_lab/shared/env_guard.py` (62 LOC) hosts `EnvironmentNotInitializedError(RuntimeError)` and `require_initialized_env(config_path, *, now=None) -> dict` — reads `config/setup.json` directly (no SDK coupling), raises with the literal `REMEDIATION` hint *"Run \`uv run init_env.py\` to populate config.hardware_constraints."* on three failure modes: missing file / null constraints / stale `captured_at`. `now` is an injectable seam for unit tests; production callers leave it `None`. (2) `src/on_prem_llm_lab/sdk/sdk.py` grew 90 → 101 LOC: added `_require_initialized_env()` helper + seven `NoReturn` stub methods (`run_plumbing_test`, `run_baseline`, `run_airllm`, `run_sweep`, `run_qlora_finetune`, `economic_analysis`, `assemble_readme`). Each stub's body is exactly `self._require_initialized_env(); raise NotImplementedError("T-X.Y lands in Mn.")` — so the precondition is enforced uniformly *today* and future M2a..M6 tasks just replace the `NotImplementedError` line while the guard stays. The two bootstrap methods (`scan_hardware`, `initialize_environment`) are correctly **exempt** from the guard (ADR-016 chicken-and-egg). (3) `init_env.py` at repo root (**26 LOC** — within the ≤30 DoD): argparse `--config` / `--repo-root`, builds `OnPremLlmSDK`, calls `initialize_environment()`, prints `Hardware scan captured at ...` + per-file `[key] status -> path (reason?)` lines + any `FAIL: ...` lines, returns 0 on success / 1 on any fail receipt. (4) `EnvironmentNotInitializedError` re-exported from `on_prem_llm_lab.sdk` AND from the root package so callers can `from on_prem_llm_lab import EnvironmentNotInitializedError`. **Tests:** `tests/unit/test_shared/test_env_guard.py` (56 LOC, 5 tests) — missing file / null constraints / fresh-passes-through / stale-raises / no-`captured_at`-raises. `tests/integration/test_env_init_required.py` (76 LOC, 3 `@pytest.mark.integration` tests) — iterates all 7 guarded SDK methods for both the null-constraints and stale-constraints fixtures, plus a positive test that `scan_hardware()` is exempt. `tests/integration/test_hardware_scanner_writes.py` (133 LOC, 3 `@pytest.mark.integration` tests) — drives the real `HardwareScanner` (with stubbed detector) against a tmp_path fixture tree (real `config/setup.json` + `docs/PRD.md` + `README.md` with placeholder pairs); asserts all three write receipts ok, asserts markers preserved verbatim with `OLD` content gone and `TestCPU`/`TestGPU` present between them, asserts `.bak` files created. Two further integration tests cover (a) byte-for-byte idempotency on second scan (receipts MUST be `ok-noop`) and (b) missing-file doc target reported as `skipped`, never `fail`. **Gates:** ruff `All checks passed!` first try; 59 files scanned, 0 LOC violations; **45 tests passed in 9.73 s** (up from 34); coverage **92.80 %** (up from 91.71 %); `env_guard.py` 100 % stmt + branch; `sdk.py` 100 % stmt. **Not run on real machine:** `uv run init_env.py` was NOT executed as part of this task — it would mutate the live `config/setup.json` + `docs/PRD.md` + `README.md` (refresh `captured_at`, update `.bak` files). T-1.14 already demonstrated the real-machine path on 2026-06-26; integration tests prove the bootstrap script logic. Run it manually whenever you want a fresh scan.
- [x] **T-1.17** 🆕 v1.20 Add the paired placeholder markers `<!-- HARDWARE_SPECS_PLACEHOLDER:START -->` / `<!-- HARDWARE_SPECS_PLACEHOLDER:END -->` to **`docs/PRD.md`** (between §1.3 and §1.4) and **`README.md`** (under "Hardware profile" section once the README template lands in M6 — placeholder block seeded now so `init_env.py` has a target on first install). — **DoD:** both files contain the marker pair exactly once; markers preserved verbatim across scanner runs. **Done 2026-06-27 (de facto, closed out alongside T-1.16):** marker pair already present exactly once in each file (`README.md` lines 12 + 24; `docs/PRD.md` lines 35 + 47 — between §1.3 and §1.3a, exactly where the DoD asked). Markers were **seeded during T-1.14's real-machine demo** on 2026-06-26 — the first `uv run on-prem-llm initialize` run found the marker pair already authored in both files (the placeholder version was committed at that point as part of the README placeholder + PRD §1.3a) and wrote the live hardware table between them. The **"preserved verbatim across scanner runs"** half of the DoD is now independently proven by the T-1.16 integration suite: `test_second_scan_is_idempotent_modulo_clock` asserts byte-for-byte equality of PRD + README after a second `scan()` with the same clock, and `test_scanner_writes_all_three_targets_end_to_end` asserts `text.count(MARK_START) == 1` and `text.count(MARK_END) == 1` after a write. `grep -c HARDWARE_SPECS_PLACEHOLDER` on both files returns **2** (one START + one END = one pair, exactly once). No new code or docs needed — this entry exists solely as the audit trail.

---

## M2a — Plumbing test green · target 2026-06-27 · 🆕 v1.10 · ADR-010 · (blocks M2b)

> Verifies the full pipeline (download → AirLLM mmap → metric collection → manifest) on a **small** model with **Q2** quantization before any oversized run. PRD FR-PT-1..3, G1, K1, SC-0.

- [x] **T-2a.1** 🆕 v1.10 Implement `services/plumbing_test_runner.py`. — **DoD:** ≤ 150 LOC; runs the four stages defined in PRD §3.10; writes `results/plumbing_<timestamp>.json` with per-stage status; raises a structured `PlumbingStageError` on failure. **Done 2026-06-27:** `src/on_prem_llm_lab/services/plumbing_test_runner.py` at exactly **150 LOC** (right at the cap; trimmed twice to land — module docstring shortened, `_stamp_for_filename` helper inlined into the f-string). **Design — dependency-injected stages.** The first three stages (`download`, `mmap_allocation`, `metric_collection`) require `ModelAcquirer` + `AirLLMBackend` + `MemorySamplingMixin`, which are still stubs in M2b/M3. To keep T-2a.1 buildable and unit-testable today without a forward dependency, the runner takes those three stages as injected zero-arg callables — same Building Block "Setup" pattern as `HardwareScanner`'s `detector` + `clock`. **T-2a.2 (next) will wire the real services into these stage callables.** Stage 4 (`manifest_write`) is the runner's own action. **Surface:** `PlumbingTestRunner(plumbing_test_model, stages, results_dir, clock=None)` returns `PlumbingResult(captured_at, plumbing_test_model, stages, overall, remediation_hint, manifest_path)`; raises `PlumbingStageError(stage, message, result)` carrying the partial result (FR-PT-3). On first-stage failure: later stages are recorded as `"skipped"` (not "fail"), the partial manifest is still persisted, then the error is raised so T-2a.4's sweep precondition can surface the stage name + remediation hint cleanly. `STAGE_ORDER` and `DEFAULT_REMEDIATION` are module-level constants (per-stage hints: "Check HF_TOKEN, network, disk", "Check airllm.layer_shards_saving_path (ADR-005)", "Check sampler/generation params", "Check results/ exists and is writable"). `StageOutcome.to_jsonable()` keeps the manifest JSON aligned with PRD §6.3 schema (omits empty `duration_s` / `error` fields, merges per-stage `extras` like `ttft_ms` / `tpot_ms`). **Tests:** `tests/unit/test_services/test_plumbing_test_runner.py` (113 LOC, 7 tests) — constructor-rejects-missing-stages, happy-path-writes-manifest, download-failure-aborts-with-structured-error-and-skips-later-stages, two parametrized mid-pipeline failure tests (`mmap_allocation` / `metric_collection`), manifest-write-failure-still-raises (monkeypatches `Path.write_text` to raise `OSError`), results-dir-auto-created-if-missing. **Gates:** ruff clean (no rule changes needed); 60 files scanned, **0 LOC violations** (plumbing_test_runner.py at exactly 150); **52 tests passed in 13.78 s** (up from 45); coverage **94.00 %** (up from 92.80 %); `plumbing_test_runner.py` itself at **99 % statement / 100 % branch** — the one uncovered line is `self._clock = clock or _utc_now_iso` (the production wall-clock fallback; unit tests always inject `_fixed_clock` so the right-hand branch never fires, which is correct behavior). **Not committed:** per the per-task stop-and-wait gate, awaiting approval before the next task; no git commit/push happened in this task.
- [x] **T-2a.2** 🆕 v1.10 Wire `OnPremLlmSDK.run_plumbing_test()` and CLI `run-plumbing-test`. — **DoD:** `uv run on-prem-llm run-plumbing-test` exits 0 on success, non-zero with a stage-specific message on failure. **Done 2026-06-27:** Wiring landed across one new service module + the SDK + the CLI + 9 new tests. (1) `src/on_prem_llm_lab/services/plumbing_default_stages.py` (**87 LOC**) hosts `build_default_stages(config, results_dir, *, hf_token=None) -> dict[str, StageCallable]` — composes the three pre-stage closures from runtime config. Heavy deps are **lazy-imported inside each closure** (`huggingface_hub.snapshot_download` inside `download`, `airllm.AutoModel` inside `mmap_allocation`, `psutil` + `time` inside `metric_collection`) so unit tests stub stages out without paying any of those import costs and `--help` / `initialize` don't trigger AirLLM. The three closures share a captured `state` dict so `mmap_allocation` sees `download`'s local path and `metric_collection` sees the model loaded by `mmap_allocation`. Stage 1 calls `snapshot_download(repo_id, token=hf_token or os.environ["HF_TOKEN"])`. Stage 2 calls `AutoModel.from_pretrained(state["model_path"], layer_shards_saving_path=airllm.layer_shards_saving_path)` (ADR-005). Stage 3 reads `gen.plumbing_prompt` + `plumbing_max_new_tokens` (defaults `"Hello, world. Briefly:"` / 32), runs `tokenizer(prompt)`, samples `psutil.Process().memory_info().rss` around two `model.generate()` calls (1-token TTFT + max-new TPOT), returns `ttft_ms` / `tpot_ms` / `peak_ram_mb` / `tokens_generated`. (2) `sdk/sdk.py` grew to **134 LOC** — `run_plumbing_test(*, stages: Mapping[str, StageCallable] | None = None) -> PlumbingResult` now: enforces the env-init guard FIRST, loads `setup.json`, builds default stages (or uses the injected ones), instantiates `PlumbingTestRunner(plumbing_test_model, stages, results_dir=repo_root/"results")`, and returns its result. `PlumbingResult` + `PlumbingStageError` re-exported from `sdk/__init__` and `on_prem_llm_lab/__init__`. (3) `cli/main.py` grew to **100 LOC** — new `run-plumbing-test` subcommand mirrors the `initialize` UX: builds the SDK with `os.environ`, calls `sdk.run_plumbing_test()`, prints `OK: plumbing test passed` + per-stage `status (duration_s)` lines on success, or formatted error blocks on `EnvironmentNotInitializedError` (one-line FAIL) or `PlumbingStageError` (stage name + message + remediation hint + partial manifest path). Exit 0 on success, 1 on either error class. (4) **Tests** — split T-1.14 SDK tests off into `test_sdk.py` (94 LOC) and moved T-2a.2 SDK tests into a new `test_sdk_plumbing.py` (77 LOC) so neither file blows the 150 LOC cap; first try landed `test_sdk.py` at 162 LOC and the file-size guard caught it correctly. New tests: `test_plumbing_default_stages.py` (37 LOC, 3 tests — factory returns the right three keys + each value is callable, missing `plumbing_test_model` raises `KeyError` at build time, missing optional `airllm` / `generation` blocks don't crash construction); `test_sdk_plumbing.py` (77 LOC, 3 tests — injected stages produce `overall='ok'` with manifest on disk, stage failure bubbles as `PlumbingStageError` with stage name set, env-init guard fires BEFORE any stage callable runs and the sentinel proves it); `test_cli/test_main.py` grew to 129 LOC (+4 tests — `--help` lists `run-plumbing-test`, success exit 0 with `OK` + stage names in stdout, stage error exit 1 with stage name + message + remediation hint in stdout, env-init error exit 1 with `env not initialised` in stdout). Closure bodies in `plumbing_default_stages.py` are deliberately NOT unit-tested (would require mocking HF + AirLLM + psutil) — they're exercised by the T-2a.3 integration test and the T-2a.5 real-machine run. **Gates:** ruff `All checks passed!` first try; **63 files scanned, 0 LOC violations** (after the test_sdk.py split); **62 tests passed in 24.56 s** (up from 52); coverage **88.64 %** (down from 94.00 % — drop is concentrated in `plumbing_default_stages.py` at 35 % stmt where the three closure bodies are uncovered by design; everything else is ≥97 %: `sdk/sdk.py` 100 %, `plumbing_test_runner.py` 100 % stmt + 100 % branch, `cli/main.py` 97 % stmt with 2 partial branches in error-formatting). Still well above the 85 % gate. **Not run on real machine:** `uv run on-prem-llm run-plumbing-test` was NOT executed as part of this task — T-2a.5 is the dedicated real-machine run task and TinyLlama-Q2 isn't downloaded yet. **Not committed:** awaiting per-task approval before commit.
- [x] **T-2a.3** 🆕 v1.10 Integration test `tests/integration/test_plumbing_test_runner.py`. — **DoD:** runs against `plumbing_test_model` (TinyLlama-Q2 by default) and asserts all four stages reach `ok`. **Done 2026-06-27:** `tests/integration/test_plumbing_test_runner.py` (**108 LOC**, 2 `@pytest.mark.integration` tests) drives the **full production wiring** end-to-end: `SDK.run_plumbing_test()` → `build_default_stages()` → `PlumbingTestRunner.run()`, with no `stages=` injection. The three heavy externals are stubbed at their import boundary inside the closures: `huggingface_hub.snapshot_download` patched via `monkeypatch.setattr` to return a tmp path; `airllm.AutoModel` injected via a fake `types.ModuleType` placed into `sys.modules["airllm"]` so the real (heavy) AirLLM package is never imported by the test process; `psutil.Process` patched with a `_FakeProcess` whose `memory_info().rss` returns 100 MB. The fake AirLLM model exposes 22 `layers` + a `_FakeTokenizer` whose `__call__(prompt, return_tensors="pt")` returns `_FakeTokenized.input_ids = [[1, 2, 3]]`, matching what the `metric_collection` closure expects. **Test 1 (`test_full_plumbing_pipeline_succeeds_end_to_end`)** — literal DoD: asserts `result.overall == "ok"`, `result.remediation_hint is None`, all four stages (`download`, `mmap_allocation`, `metric_collection`, `manifest_write`) reach `status == "ok"`, manifest exists on disk under `tmp_path/results/`, manifest JSON contains `plumbing_test_model.id == "TinyLlama/TinyLlama-1.1B-Chat-v1.0"`, `stages.mmap_allocation.layers == 22`, and `ttft_ms` + `peak_ram_mb` present in `stages.metric_collection`. **Test 2 (`test_cli_run_plumbing_test_succeeds_end_to_end`)** — same end-to-end stubbing but invoked through the CLI: `CliRunner().invoke(app, ["run-plumbing-test", "--config", ..., "--repo-root", ...])` must exit 0, print `"OK: plumbing test passed"`, and list all four stage names in stdout. This complements the existing unit-level CLI tests in `test_cli/test_main.py` (which monkeypatched at the SDK level) by proving the SDK ↔ CLI ↔ runner ↔ default-stages factory wire-up is sound through every layer. **Fixture builder `_make_setup(tmp_path)`** writes a `setup.json` with `hardware_constraints.captured_at = 2026-06-27T10:00:00Z` + `init.max_age_hours = 99999` so the env-init guard passes, plus a `generation.plumbing_max_new_tokens = 4` to keep the simulated work tiny. **Gates:** ruff `All checks passed!` first try; **64 files scanned, 0 LOC violations** (integration file at 108 LOC); **64 tests passed in 12.64 s** (up from 62 — 2 new integration tests; pytest runtime actually dropped from 24.56 s because the import warm-up was already paid by earlier tests); coverage **94.24 %** (up from 88.64 % — the new tests fill in `plumbing_default_stages.py` from 35 % → 95 % stmt by exercising the three closure bodies end-to-end; only line 80 `raise RuntimeError("AirLLM model has no tokenizer attached")` remains uncovered, which is the error path for the malformed-model case — fine to leave for T-2a.5 real-machine, or for a future focused unit test). Module-by-module coverage now: `sdk/sdk.py` 100 %, `plumbing_test_runner.py` 100 % stmt + 100 % branch, `plumbing_default_stages.py` 95 % stmt, `cli/main.py` 97 % stmt. **Real `plumbing_test_model` not yet downloaded** — that's T-2a.5's job; this task proves the wiring is sound without taking the 1–2 GB hit. **Not committed:** awaiting per-task approval.
- [x] **T-2a.4** 🆕 v1.10 Add `SDK.run_sweep` precondition: requires a current plumbing manifest unless `skip_plumbing=True`. — **DoD:** unit test asserts `PlumbingNotRunError` raised when manifest absent; integration test `tests/integration/test_sweep_requires_plumbing.py` confirms. **Done 2026-06-27:** Built as a guard module + SDK wiring + 12 new tests. (1) New `src/on_prem_llm_lab/shared/plumbing_guard.py` (**42 LOC**) — mirrors the `env_guard.py` shape: `PlumbingNotRunError(RuntimeError)` exception, module-level `REMEDIATION` constant ("Run `uv run on-prem-llm run-plumbing-test` first, or pass skip_plumbing=True."), and `require_current_plumbing(results_dir: Path) -> Path` which globs `plumbing_*.json`, sorts (lex-sortable since the runner stamps the filename with ISO digits — confirmed against `PlumbingTestRunner.run`'s naming), takes the latest, parses it, and raises `PlumbingNotRunError` on any of: empty glob ("No plumbing manifest under {results_dir}"), `OSError`/`json.JSONDecodeError` chained as "Failed to read plumbing manifest", or `overall != "ok"` ("Latest plumbing manifest {name} has overall={value!r}"). Missing `overall` field is treated as failure (defensive — surfaces as `overall=None`). Returns the path of the latest ok manifest on success so future callers (T-3.5) can record the manifest pointer in their own outputs. (2) `sdk/sdk.py` (**149 LOC** — was 134, grew 15 with the import + `_require_current_plumbing()` helper + `run_sweep` signature change + precondition body; trimmed the module docstring's historical paragraph by 6 LOC to land at 149 = one under the cap). `run_sweep(prompts, *, skip_plumbing: bool = False, **kwargs)` now: env-init guard first, then plumbing guard (unless `skip_plumbing`), then the existing `NotImplementedError("T-3.5 lands in M3.")`. `_require_current_plumbing()` helper added for symmetry with `_require_initialized_env()`. `PlumbingNotRunError` re-exported from `sdk/sdk.py.__all__`, `sdk/__init__.py`, and `on_prem_llm_lab/__init__.py` — callers can `from on_prem_llm_lab import PlumbingNotRunError`. (3) **Unit tests** — `tests/unit/test_shared/test_plumbing_guard.py` (**52 LOC**, 5 tests) covers: empty results dir raises with literal "No plumbing manifest" match; multi-manifest latest=ok returns the latest path (proving lex sort = chronological); older=ok + latest=fail still raises with `overall='fail'` match (proves precondition isn't fooled by a stale success); missing `overall` field raises with `overall=None`; corrupt JSON raises with "Failed to read". `tests/unit/test_sdk/test_sdk_sweep.py` (**64 LOC**, 4 tests — literal DoD coverage at the SDK boundary) covers: no manifest → `PlumbingNotRunError`; `skip_plumbing=True` → reaches `NotImplementedError("T-3.5")`; ok manifest present → reaches `NotImplementedError("T-3.5")` (proves the guard PASSES when satisfied); env guard fires before plumbing guard when env is uninitialised (precedence assertion). (4) **Integration test** — `tests/integration/test_sweep_requires_plumbing.py` (**96 LOC**, 3 `@pytest.mark.integration` tests) — the file path requested by the DoD verbatim. Reuses the same `_stub_externals` pattern from `test_plumbing_test_runner.py` (`huggingface_hub.snapshot_download` patched, `airllm` module faked via `sys.modules` injection, `psutil.Process` stubbed). Test 1: no manifest end-to-end → `PlumbingNotRunError` with "No plumbing manifest" match. Test 2: **runs a real `PlumbingTestRunner` first** via `sdk.run_plumbing_test()` (asserts `result.overall == "ok"` + `manifest_path.exists()`), then `sdk.run_sweep()` reaches `NotImplementedError("T-3.5")` — proving the real manifest produced by the runner is the same shape the guard accepts. Test 3: `skip_plumbing=True` reaches `NotImplementedError("T-3.5")` even with empty results dir. **Gates:** ruff `All checks passed!` first try; **68 files scanned, 0 LOC violations** after a one-pass docstring trim on `sdk/sdk.py` (file-size guard caught the 154 → fixed to 149); **76 tests passed in 10.32 s** (up from 64); coverage **94.50 %** (up from 94.24 %). New module coverage: `shared/plumbing_guard.py` 100 % stmt + 100 % branch (5 unit tests cover all 4 raise paths + the happy return); `sdk/sdk.py` still 100 %. **Not committed:** awaiting per-task approval.
- [ ] **T-2a.5** 🆕 v1.10 Execute the plumbing test on the student's actual machine and commit the resulting manifest under `results/`. — **DoD:** real manifest committed; status `overall: ok`.

---

## M2b — Baseline captured · target 2026-06-28

### 2b.A — Backends infra
- [ ] **T-2.0** 🆕 v1.10 Implement static AST guard `tests/unit/test_backends/test_init_uses_automodel.py` that blocks imports of concrete `*ForCausalLM` classes in `backends/` (ADR-009 enforcement). — **DoD:** test fails on a fixture that imports `LlamaForCausalLM`; passes on real code.
- [ ] **T-2.1** Define `backends/base.py` (`InferenceBackend` ABC, `BackendRunResult` dataclass, `Quant` enum). — **DoD:** ≤ 150 LOC; unit tests for the dataclass.
- [ ] **T-2.2** Implement `mixins/timing_mixin.py`. — **DoD:** isolated unit tests; computes TTFT and TPOT correctly on a faked generator.
- [ ] **T-2.3** Implement `mixins/memory_sampling_mixin.py`. — **DoD:** background thread sampling at configurable Hz, thread-safe stop, returns peak RAM/VRAM; unit-tested with a fake sampler.
- [ ] **T-2.4** Implement `mixins/manifest_logging_mixin.py`. — **DoD:** writes a JSON manifest with seed/config snapshot/git hash; includes `target_label` and `model_id`.
- [ ] **T-2.5** Implement `mixins/energy_accounting_mixin.py`. — **DoD:** computes Wh from configured wattage × wall_s.

### 2b.B — API Gatekeeper 🔒 constitution §4
- [ ] **T-2.6** Implement `shared/gatekeeper.py` (`ApiGatekeeper`, `RateLimitConfig`, `QueueStatus`, `GatekeeperError`). — **DoD:** FIFO queue, max depth from config, retry/backoff from config, structured JSON logging; ≤ 150 LOC across (split into helper file if needed).
- [ ] **T-2.7** Integration test `tests/integration/test_gatekeeper_queue.py` — burst beyond limit, assert FIFO, no crash, queue drains.
- [ ] **NOTE:** Author **`docs/PRD_api_gatekeeper.md`** alongside this work (see §7 below).

### 2b.C — Direct back-end + baseline run
- [ ] **T-2.8** Implement `backends/direct_backend.py` using **`AutoModelForCausalLM`** via `shared/automodel_factory.py` (ADR-009). Accepts `target_label` + `quantization`. — **DoD:** ≤ 150 LOC; unit tests with a tiny stub model; static AST test (T-2.0) passes.
- [ ] **T-2.9** Implement `services/benchmark_runner.py` (orchestrates load → sample-on → generate → sample-off → unload → manifest). — **DoD:** unit + integration tests.
- [ ] **T-2.10** Wire `SDK.run_baseline(target_label, ...)` and `cli run-baseline`. — **DoD:** executes against **each** oversized model in `config.target_models`; failure / extreme latency captured under `results/baseline_<label>_*`; logged, not silently swallowed (PRD SC-1).

---

## M3 — AirLLM + Quantization sweep · target 2026-06-29

### 3.A — AirLLM back-end (**central mechanism** — separate PRD required, see §7)
- [ ] **T-3.1** Implement `backends/airllm_backend.py`. Honors `airllm.layer_shards_saving_path` from config; validates free disk via `HardwareProfiler` precondition (ADR-005). Uses `AutoTokenizer` (ADR-009). — **DoD:** ≤ 150 LOC; unit-tested with mocked AirLLM; integration-tested on the plumbing-test model.
- [ ] **T-3.2** Author **`docs/PRD_airllm_integration.md`** (see §7).

### 3.B — Quantization (**central mechanism** — separate PRD required, see §7)
- [ ] **T-3.3** Add quantization adapter logic per back-end (FP16 / Q8 / Q4 mandatory; Q2 covered by plumbing + aggressive end; NF4 reserved for QLoRA M5). — **DoD:** SweepRunner can iterate the matrix; each backend either supports a level or raises `UnsupportedQuantization`.
- [ ] **T-3.4** Author **`docs/PRD_quantization.md`** (see §7).

### 3.C — Sweep runner & quality matrix
- [ ] **T-3.5** 🆕 v1.10 Implement `services/sweep_runner.py` — **iterates over `config.target_models` (ADR-013) × quantizations × backends**, with N repeats and warm-up, and asserts plumbing precondition (ADR-010). — **DoD:** ≤ 150 LOC; writes `results/sweep_<timestamp>.csv` + manifest; rows carry `target_label`.
- [ ] **T-3.6** Implement quality-matrix harness — same prompt, all quantizations, **per target model**, side-by-side outputs → `results/quality_matrix.md`. — **DoD:** referenced from README.

### 3.D — Plots
- [ ] **T-3.7** Add `services/plot_style.py` (style constants only) and chart helpers for: TTFT vs quantization, TPOT vs quantization, Throughput vs quantization, peak RAM/VRAM vs quantization, Roofline-style chart per assignment §3 — **all charts faceted by `target_label`**. — **DoD:** PNGs land in `figures/`; ≤ 150 LOC each helper file.

### 3.E — Config & shared
- [ ] **T-3.8** Implement `shared/config.py.load(config_dir)` and Pydantic/dataclass schemas — validates `version` on each JSON file; validates `target_models[]` schema (unique labels, valid quantizations). — **DoD:** unit tests for happy + version-mismatch + duplicate-label + invalid-quantization paths.
- [ ] **T-3.9** Implement `shared/logging_setup.py` (JSON-line file handler). — **DoD:** unit tests; honored by Gatekeeper.

---

## M4 — Economic analysis & 3-curve break-even · target 2026-06-30

- [ ] **T-4.1** Implement `services/economic_analyzer.py` — computes per-request Anthropic API cost, On-Prem amortized cost, **Cloud GPU per-request cost** (ADR-012), and the break-even crossover(s). — **DoD:** ≤ 150 LOC; unit-tested with synthetic inputs; honors PRD FR-12, FR-13, FR-13a, FR-14.
- [ ] **T-4.2** 🆕 v1.10 Implement `backends/api_backend.py` — **Anthropic Messages API only** (ADR-011), routed through Gatekeeper. — **DoD:** ≤ 150 LOC; unit-tested with mocked `anthropic` client.
- [ ] **T-4.3** 🆕 v1.10 Update `config/api_pricing.json` with current published **Anthropic** prices and `captured_at` date; cite source URL in `docs/prompts_book.md` and in the README assumption table. — **DoD:** numbers reflect the date of capture; placeholders removed.
- [ ] **T-4.4** 🆕 v1.10 Generate `figures/break_even.png` with **three curves** (On-Prem, Anthropic, Cloud GPU) and labeled crossover points, plus the assumption table in README. — **DoD:** all three series present; assumptions visible (electricity rate, capex, lifetime, wattage, cloud hourly rate, Anthropic prices); reproducible from CSVs.
- [ ] **T-4.5** Author **`docs/PRD_economic_analysis.md`** (see §7).

---

## M5 — §5.7 QLoRA training extension · target 2026-06-30 · 🆕 v1.10 LOCKED

> Q3 resolved: §5.7 extension = **QLoRA fine-tune** on one of the target models. The two alternative options from v1.00 (extra metric, cross-model comparison) are dropped.

- [ ] **T-5.0** 🆕 v1.10 **(blocks T-5.1..T-5.4)** Author **`docs/PRD_qlora.md`** BEFORE implementation begins (constitution §1.3, ADR-014). Must include: theoretical background (NF4 / Double Quantization / Paged Optimizers per L08 §5.1; LoRA per L08 §7.3), I/O contract (`target_label`, dataset, `LoraConfig` → `QloraReport`), constraints (tiny dataset, VRAM budget), alternatives considered (full FT, plain LoRA), success criteria (training completes, train-VRAM ≈ 3–5× inference per L08 §7.1), test scenarios. — **DoD:** file exists; user reviewed; status approved.
- [ ] **T-5.1** Implement `services/qlora_trainer.py` (Building Block per ADR-014). — **DoD:** ≤ 150 LOC; uses `peft.LoraConfig`, `bitsandbytes` NF4; loads base model via `AutoModelForCausalLM` (ADR-009); unit-tested with mocked PEFT.
- [ ] **T-5.2** Wire `SDK.run_qlora_finetune(...)` and CLI `run-qlora`. — **DoD:** end-to-end run on the smallest target model completes; emits `results/qlora_<label>_<timestamp>.json` with train + inference VRAM peaks.
- [ ] **T-5.3** Generate `figures/qlora_vram_ratio.png` — bar chart of training VRAM vs inference VRAM for the chosen target. — **DoD:** chart present in README §QLoRA.
- [ ] **T-5.4** Author the README §"QLoRA fine-tune extension" with motivation, NF4 explanation, method, dataset description, results, and connection to L08 §7. — **DoD:** section present; cites `docs/PRD_qlora.md`.

---

## M6 — README assembled · target 2026-07-01

- [ ] **T-6.1** Implement `services/report_assembler.py` — interpolates AUTOGEN regions in `README.md` from `results/` and `figures/` (ADR-007). — **DoD:** ≤ 150 LOC; idempotent; golden-file integration test.
- [ ] **T-6.2** Author the README template skeleton — sections: Project intro, Hardware profile, Methodology, **Plumbing test write-up (🆕 v1.10)**, Baseline (failure narrative, per target), AirLLM rescue (per target), Quantization sweep (per target), Quality matrix, **Economic analysis (3-curve break-even — 🆕 v1.10)**, Theory linkage (Prefill/Decode/Roofline/Paging), **QLoRA extension section (🆕 v1.10)**, Reproducibility (commands), Configuration, License & Credits. — **DoD:** every PRD K1..K14 visible in the rendered README; assignment §8 README requirements all present.
- [ ] **T-6.3** Author a top-level `README.md` user-manual section per constitution §1.1 (Installation, Usage, Examples, Configuration Guide, Contribution Guidelines, License & Credits). — **DoD:** all six subsections present.

---

## M7 — Quality gates green · target 2026-07-01

- [ ] **T-7.1** `uv run ruff check src/ tests/` returns 0 violations. — **DoD:** clean.
- [ ] **T-7.2** `uv run pytest --cov` returns coverage ≥ 85 %. — **DoD:** `fail_under` enforced.
- [ ] **T-7.3** `uv run python tools/check_file_size.py` returns 0 violations. — **DoD:** all files ≤ 150 LOC.
- [ ] **T-7.4** Secrets scan (grep for likely token patterns + manual review). — **DoD:** zero matches; `.env-example` present with `HF_TOKEN` + `ANTHROPIC_API_KEY` only.
- [ ] **T-7.5** 🆕 v1.10 Grep guard: **no model-ID literals in `src/`** outside `shared/config.py`'s reader (ADR-013). — **DoD:** CI step returns 0.
- [ ] **T-7.6** 🆕 v1.10 Grep guard: **no concrete `*ForCausalLM` imports in `src/backends/`** (ADR-009). — **DoD:** static AST test (T-2.0) passes.
- [ ] **T-7.7** Repository checklist per constitution §16 — every checkbox ticked or explicitly waived in `docs/prompts_book.md`. — **DoD:** §16 checklist annotated.

---

## M8 — Submission · target 2026-07-01

- [ ] **T-8.1** Tag `v1.00` on the main branch. — **DoD:** annotated git tag; CHANGELOG entry (optional).
- [ ] **T-8.2** Final smoke test: clone clean, `uv sync`, `uv run on-prem-llm profile-hardware`, `uv run on-prem-llm run-plumbing-test`, `uv run on-prem-llm run-sweep --dry-run`. — **DoD:** all four commands succeed on a fresh checkout.
- [ ] **T-8.3** Submission per assignment §7 (GitHub repo URL). — **DoD:** repo accessible; README renders correctly on GitHub.

---

## 7. Dedicated PRDs to author (constitution §1.3 mandatory) — REVISED v1.10

> Central mechanisms and complex algorithms **MUST** receive their own PRD: theory background, requirements, I/O, constraints, alternatives considered, success criteria, test scenarios (constitution §1.3). Create these documents as the corresponding milestones begin — they are themselves mandatory deliverables.

| ID | File | Status | When | Why this needs its own PRD |
|----|------|--------|------|----------------------------|
| DP-1 | `docs/PRD_api_gatekeeper.md` | **MANDATORY** | M2b (with T-2.6) | Centralized rate-limit + queue mechanism; constitutionally mandated (§4), and the project's core safety net for outbound calls. |
| DP-2 | `docs/PRD_airllm_integration.md` | **MANDATORY** | M3 (with T-3.1) | Layer-by-layer execution + `mmap` + OS-paging analogy is the headline algorithm (L08 §8); requires theory, IO contract, fault modes, success criteria. |
| DP-3 | `docs/PRD_quantization.md` | **MANDATORY** | M3 (with T-3.3) | Multi-level bit-width policy (FP16 / Q8 / Q4 / Q2 / NF4); requires per-level acceptance thresholds and quality-cliff documentation (L08 §5). |
| DP-4 | `docs/PRD_benchmarking_methodology.md` | **MANDATORY** | M2b (with T-2.9) | TTFT / TPOT / Throughput / peak-memory measurement methodology — non-trivial, must define exactly what we count, how we warm up, repetitions, statistics. |
| DP-5 | `docs/PRD_economic_analysis.md` | **MANDATORY** | M4 (with T-4.5) | Break-even calculation across **three curves** (Anthropic API, On-Prem, Cloud GPU); assumptions table, formula derivation, sensitivity to electricity rate, lifetime, cloud hourly rate. |
| DP-6 | `docs/PRD_roofline_analysis.md` | **MANDATORY** | M3 (with T-3.7) | Roofline model that proves compute-bound vs memory-bound (assignment §3) — theoretical derivation referencing Prefill (GEMM) and Decode (GEMV) regimes (L08 §3). |
| DP-7 | **`docs/PRD_qlora.md`** | **🆕 v1.10 MANDATORY** | M5 (with T-5.0 — **blocks T-5.1**) | QLoRA fine-tune is the §5.7 extension (Q3) and is a central mechanism (NF4, Double Quantization, Paged Optimizers, LoRA adapters) — constitution §1.3 mandates a dedicated PRD; ADR-014 confirms boundary. |
| DP-8 | `docs/PRD_plumbing_test.md` | **🆕 v1.10 RECOMMENDED** | M2a (with T-2a.1) | The plumbing-first execution rule (ADR-010) is itself a non-trivial mechanism: defines per-stage contracts, failure remediation hints, manifest schema, integration into `run_sweep` precondition. May be waived if `PlumbingTestRunner` stays ≤ 150 LOC and is fully covered by tests + ADR-010. |
| DP-9 | `docs/PRD_report_assembler.md` | OPTIONAL | M6 (with T-6.1) | Auto-interpolation of README sections from results is a non-trivial mechanism; PRD recommended but may be waived if implementation stays ≤ 150 LOC and is fully covered by tests. |
| DP-10 | `docs/PRD_hardware_scanner.md` | **🆕 v1.20 RECOMMENDED** | M1 (with T-1.13) | HardwareScanner has three write side-effects across two file types (JSON config + Markdown docs) with atomic semantics and an invariant about who else may edit the placeholder regions (nobody). May be waived if `services/hardware_scanner.py` stays ≤ 150 LOC AND the integration test `test_hardware_scanner_writes.py` covers all four outcomes (ok / skipped-missing-file / skipped-missing-marker / fail). |

> v1.20 changes vs v1.10: added `PRD_hardware_scanner.md` (recommended). v1.10 changes vs v1.00: `PRD_qlora.md` is now MANDATORY (was conditional on selecting QLoRA — Q3 resolved that selection). New entry `PRD_plumbing_test.md` (recommended). All other entries unchanged in mandatory/optional status.

---

## 8. Mandatory infrastructure tasks recap (constitution gates)

> These items are repeated here for visibility; each maps to a task above.

| Gate | Constitution ref. | Task |
|------|-------------------|------|
| `uv` only (no `pip`, no `venv`, no `python -m`, no `virtualenv`) | §7.4 | T-1.1..T-1.3 |
| `uv.lock` committed | §7.4 | T-1.2 |
| `ruff check` returns 0 | §6.1 | T-1.4, T-7.1 |
| TDD with coverage ≥ 85 % | §5.2 | T-1.5, every implementation task, T-7.2 |
| Files ≤ 150 LOC | §2.2 | T-1.6, T-7.3 |
| No hard-coded values (incl. **target model IDs as JSON array** — 🆕 v1.10) | §6.2 | T-1.9 + every config consumer + T-7.5 |
| No secrets in code; `.env-example` committed (HF + Anthropic only — 🆕 v1.10) | §6.4 | T-1.7, T-1.8, T-7.4 |
| Versioning `1.00` on code + each JSON config | §7.1 | T-1.9, T-1.11, T-3.8 |
| All API calls via Gatekeeper | §4 | T-2.6, T-2.7, DP-1 |
| All business logic via SDK; CLI delegate-only | §3.1 | T-1.12, every SDK method binding |
| Prompts Book maintained | §7.3 | T-1.3, T-4.3, T-7.7 (`docs/prompts_book.md`) |
| **`AutoModel*` init only** (🆕 v1.10) | NFR-16 / ADR-009 | T-1.15, T-2.0, T-2.8, T-3.1, T-7.6 |
| **Plumbing-first** (🆕 v1.10) | FR-PT-1..3 / ADR-010 | M2a all tasks, T-2a.4 |
| **3-curve break-even chart** (🆕 v1.10) | FR-14 / ADR-012 | T-4.1, T-4.4 |
| **Anthropic-only API** (🆕 v1.10) | FR-6 / ADR-011 | T-1.8, T-4.2, T-4.3 |
| **`docs/PRD_qlora.md` before code** (🆕 v1.10) | §1.3 / ADR-014 | T-5.0 (blocks T-5.1..T-5.4) |
| **HardwareScanner auto-injection + atomic writes** (🆕 v1.20) | ADR-015 | T-1.13, T-1.17 |
| **`init_env.py` env-init-first** (🆕 v1.20) | ADR-016 | T-1.16; precondition guard wired into every other SDK method |

---

## 9. Phase boundaries (DoD per milestone)

- **M1 done when:** `uv sync` works, ruff clean, pytest runs, **HardwareScanner writes config + patches PRD + README placeholders, `init_env.py` runs end-to-end (`uv run init_env.py`), `config.hardware_constraints` is populated**, `automodel_factory.py` is in place.
- **M2a done when:** plumbing test passes on the student's machine and the manifest is committed under `results/`.
- **M2b done when:** baseline run on each oversized target is captured (success or documented failure); Gatekeeper integration test passes; AST guard for `AutoModel*` is green.
- **M3 done when:** sweep CSV exists with rows for every `(target_label × quantization)` combination on the AirLLM back-end; all sweep charts in `figures/`; PRD_airllm_integration + PRD_quantization + PRD_benchmarking_methodology + PRD_roofline_analysis exist.
- **M4 done when:** `figures/break_even.png` exists with **three labeled curves** (On-Prem, Anthropic, Cloud GPU) and marked crossovers; `PRD_economic_analysis.md` exists.
- **M5 done when:** `docs/PRD_qlora.md` is approved; QLoRA training completes; train/inference VRAM ratio chart present in README.
- **M6 done when:** README is fully assembled and renders correctly; all required sections per assignment §8 present; QLoRA + plumbing + 3-curve break-even sections all included.
- **M7 done when:** all six quality gates green (ruff, coverage, file-size, secrets scan, no-model-ID literals, no concrete `*ForCausalLM` imports); constitution §16 checklist annotated.
- **M8 done when:** `v1.00` tag pushed; smoke test passes from a fresh clone, including `run-plumbing-test`.

---

## 10. Approval

This document **MUST** be approved by the user — alongside PRD v1.10 and PLAN v1.10 — before any code is written (constitution §1.5).

> Approval status: ☐ Pending user review of revision v1.10.
